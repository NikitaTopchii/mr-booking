{
  "name": "rooms-infrastructure",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/rooms/server/infrastructure/src",
  "projectType": "library",
  "tags": ["scope:rooms", "type:infrastructure", "platform:server"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/libs/rooms/server/infrastructure",
        "main": "libs/rooms/server/infrastructure/src/index.ts",
        "tsConfig": "libs/rooms/server/infrastructure/tsconfig.lib.json",
        "assets": []
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/rooms/server/infrastructure/tsconfig.lib.json --noEmit --rootDir ."
      }
    }
  }
}
